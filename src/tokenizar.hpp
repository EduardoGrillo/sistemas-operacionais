#ifndef TOKENIZAR_H
#define TOKENIZAR_H

#include <iostream>
#include <sstream>

using namespace std;

class Tokenizar{
    private:

    public:

    Tokenizar();
    ~Tokenizar();

    void tok(string text, int contLinha);
};

#endif